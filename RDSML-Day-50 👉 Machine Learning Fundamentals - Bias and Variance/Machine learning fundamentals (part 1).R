# What is bias and covariance in machine learning?
# Linear regression and squiggly line regression


# Variance: In machine learning lingo, the difference in fits between data sets -
            # is called variance

# Bias: The inability of a machine learning method (like linear regression) -
          # to capture the true relationship is called bias


# We will prefer on a model that has low variance but can have high biases rather
    # a model with high variance with low bias